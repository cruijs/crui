{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "version": "lerna exec -- tsc --noEmit && lerna version",
    "publish": "lerna publish from-package"
  }
}
